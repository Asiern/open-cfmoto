// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v6.33.5
// source: meter.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "";

/** Auth step 1 — App→Bike (control 0x5A) */
export interface AuthPackage {
  info: Uint8Array;
}

/** Auth step 2 — Bike→App (control 0x5B): bike random challenge */
export interface TboxRandomNum {
  codec: Uint8Array;
}

/** Auth step 3 — App→Bike (control 0x5C): decrypted challenge response */
export interface RandomNum {
  sn: string;
}

/** Auth result — Bike→App (control 0x5D): 0 = success */
export interface TboxAuthResult {
  result: number;
}

/** Keep-alive ping — App→Bike (control 0x67, ping=1) */
export interface Heartbeat {
  ping: number;
}

/** Lock/unlock/power — App→Bike (control 0x67) */
export interface Lock {
  type: Lock_Type;
  state: Lock_State;
}

export enum Lock_Type {
  UNUSE1 = 0,
  MOTORCYCLE = 1,
  SADDLE = 2,
  MAIN_STAND = 3,
  STORAGE_BOX = 4,
  SIDE_BOX = 5,
  TAIL_BOX = 6,
  POWER_ON_OFF = 7,
  UNRECOGNIZED = -1,
}

export enum Lock_State {
  UNUSE2 = 0,
  UNLOCKED = 1,
  LOCKED = 2,
  POWER_ON = 3,
  POWER_OFF = 4,
  UNRECOGNIZED = -1,
}

/** Find car (flash/horn/headlight) — App→Bike (control 0x6A) */
export interface FindCar {
  headlightStatus: boolean;
  doubleflashStatus: boolean;
  loudspeakerStatus: boolean;
}

/** Turn signal control — App→Bike (control 0x6B) */
export interface LightControl {
  type: LightControl_Type;
}

export enum LightControl_Type {
  NONE2 = 0,
  RIGHT_OPEN = 1,
  RIGHT_CLOSE = 2,
  LEFT_OPEN = 3,
  LEFT_CLOSE = 4,
  UNRECOGNIZED = -1,
}

/** Ignition — App→Bike (control 0x79) */
export interface KL15 {
  kL15: number;
}

/** 4G module command — App→Bike (control 0x0A) */
export interface Operate4g {
  command: number;
  body: number;
  msgId: number;
}

/** 4G module complex command — App→Bike (control 0x0C) */
export interface Operate4gComplex {
  command: number;
  body: string;
  msgId: number;
}

/** Enable/disable charging — App→Bike (control 0x0B) */
export interface ChargeStatus {
  enableCharge: boolean;
}

/** Charge power setting — App→Bike (control 0x71) */
export interface ChargeSetting {
  chargePower: number;
}

/** Max speed limit — App→Bike (control 0x68) */
export interface Preference {
  maximumSpeedLimit: number;
}

/** Display units — App→Bike (control 0x69) */
export interface Display {
  distance: Display_Distance;
  temperature: Display_Temperature;
  time: Display_Time;
  languageType: Display_Language;
}

export enum Display_Distance {
  NONE1 = 0,
  KM = 1,
  MILE = 2,
  UNRECOGNIZED = -1,
}

export enum Display_Temperature {
  NONE2 = 0,
  CELSIUS = 1,
  FAHRENHEIT = 2,
  UNRECOGNIZED = -1,
}

export enum Display_Time {
  NONE3 = 0,
  H12 = 1,
  H24 = 2,
  UNRECOGNIZED = -1,
}

export enum Display_Language {
  NONE4 = 0,
  CN = 1,
  EN = 2,
  UNRECOGNIZED = -1,
}

/** Charger info query — App→Bike (control 0x15) */
export interface PatchObtainInfoControl {
  groupId: number;
}

/** Charger info response — Bike→App (control 0x95) */
export interface PatchObtainInfoResult {
  chargerConnState: number;
  chargState: number;
}

/**
 * Generic command result — Bike→App (0xE7, 0xEA, 0xEB, 0xF9, 0x8B)
 * errRes on LockControll: 0/1=unlock, 16/17=lock, 32/33=powerOn, 48/49=powerOff
 */
export interface CommandResult {
  result: number;
  errRes: number;
}

/** 4G command result — Bike→App (0x8A, 0x8C) */
export interface CommandResult2 {
  result: number;
  errorCode: number;
}

/** Display theme — App→Bike (control 0x65) */
export interface Theme {
  themes: number;
}

/** Navigation — App→Bike (control 0x66) */
export interface Navi {
  direction: number;
  distance: number;
}

function createBaseAuthPackage(): AuthPackage {
  return { info: new Uint8Array(0) };
}

export const AuthPackage: MessageFns<AuthPackage> = {
  encode(message: AuthPackage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.info.length !== 0) {
      writer.uint32(10).bytes(message.info);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuthPackage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthPackage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.info = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<AuthPackage>, I>>(base?: I): AuthPackage {
    return AuthPackage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AuthPackage>, I>>(object: I): AuthPackage {
    const message = createBaseAuthPackage();
    message.info = object.info ?? new Uint8Array(0);
    return message;
  },
};

function createBaseTboxRandomNum(): TboxRandomNum {
  return { codec: new Uint8Array(0) };
}

export const TboxRandomNum: MessageFns<TboxRandomNum> = {
  encode(message: TboxRandomNum, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.codec.length !== 0) {
      writer.uint32(10).bytes(message.codec);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TboxRandomNum {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTboxRandomNum();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.codec = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<TboxRandomNum>, I>>(base?: I): TboxRandomNum {
    return TboxRandomNum.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TboxRandomNum>, I>>(object: I): TboxRandomNum {
    const message = createBaseTboxRandomNum();
    message.codec = object.codec ?? new Uint8Array(0);
    return message;
  },
};

function createBaseRandomNum(): RandomNum {
  return { sn: "" };
}

export const RandomNum: MessageFns<RandomNum> = {
  encode(message: RandomNum, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sn !== "") {
      writer.uint32(10).string(message.sn);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RandomNum {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRandomNum();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sn = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<RandomNum>, I>>(base?: I): RandomNum {
    return RandomNum.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RandomNum>, I>>(object: I): RandomNum {
    const message = createBaseRandomNum();
    message.sn = object.sn ?? "";
    return message;
  },
};

function createBaseTboxAuthResult(): TboxAuthResult {
  return { result: 0 };
}

export const TboxAuthResult: MessageFns<TboxAuthResult> = {
  encode(message: TboxAuthResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.result !== 0) {
      writer.uint32(8).int32(message.result);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TboxAuthResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTboxAuthResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.result = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<TboxAuthResult>, I>>(base?: I): TboxAuthResult {
    return TboxAuthResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TboxAuthResult>, I>>(object: I): TboxAuthResult {
    const message = createBaseTboxAuthResult();
    message.result = object.result ?? 0;
    return message;
  },
};

function createBaseHeartbeat(): Heartbeat {
  return { ping: 0 };
}

export const Heartbeat: MessageFns<Heartbeat> = {
  encode(message: Heartbeat, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ping !== 0) {
      writer.uint32(8).int32(message.ping);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Heartbeat {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHeartbeat();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ping = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<Heartbeat>, I>>(base?: I): Heartbeat {
    return Heartbeat.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Heartbeat>, I>>(object: I): Heartbeat {
    const message = createBaseHeartbeat();
    message.ping = object.ping ?? 0;
    return message;
  },
};

function createBaseLock(): Lock {
  return { type: 0, state: 0 };
}

export const Lock: MessageFns<Lock> = {
  encode(message: Lock, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== 0) {
      writer.uint32(8).int32(message.type);
    }
    if (message.state !== 0) {
      writer.uint32(16).int32(message.state);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Lock {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLock();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<Lock>, I>>(base?: I): Lock {
    return Lock.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Lock>, I>>(object: I): Lock {
    const message = createBaseLock();
    message.type = object.type ?? 0;
    message.state = object.state ?? 0;
    return message;
  },
};

function createBaseFindCar(): FindCar {
  return { headlightStatus: false, doubleflashStatus: false, loudspeakerStatus: false };
}

export const FindCar: MessageFns<FindCar> = {
  encode(message: FindCar, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.headlightStatus !== false) {
      writer.uint32(8).bool(message.headlightStatus);
    }
    if (message.doubleflashStatus !== false) {
      writer.uint32(16).bool(message.doubleflashStatus);
    }
    if (message.loudspeakerStatus !== false) {
      writer.uint32(24).bool(message.loudspeakerStatus);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FindCar {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFindCar();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.headlightStatus = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.doubleflashStatus = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.loudspeakerStatus = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<FindCar>, I>>(base?: I): FindCar {
    return FindCar.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FindCar>, I>>(object: I): FindCar {
    const message = createBaseFindCar();
    message.headlightStatus = object.headlightStatus ?? false;
    message.doubleflashStatus = object.doubleflashStatus ?? false;
    message.loudspeakerStatus = object.loudspeakerStatus ?? false;
    return message;
  },
};

function createBaseLightControl(): LightControl {
  return { type: 0 };
}

export const LightControl: MessageFns<LightControl> = {
  encode(message: LightControl, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== 0) {
      writer.uint32(8).int32(message.type);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LightControl {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLightControl();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<LightControl>, I>>(base?: I): LightControl {
    return LightControl.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LightControl>, I>>(object: I): LightControl {
    const message = createBaseLightControl();
    message.type = object.type ?? 0;
    return message;
  },
};

function createBaseKL15(): KL15 {
  return { kL15: 0 };
}

export const KL15: MessageFns<KL15> = {
  encode(message: KL15, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.kL15 !== 0) {
      writer.uint32(8).int32(message.kL15);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KL15 {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKL15();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.kL15 = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<KL15>, I>>(base?: I): KL15 {
    return KL15.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KL15>, I>>(object: I): KL15 {
    const message = createBaseKL15();
    message.kL15 = object.kL15 ?? 0;
    return message;
  },
};

function createBaseOperate4g(): Operate4g {
  return { command: 0, body: 0, msgId: 0 };
}

export const Operate4g: MessageFns<Operate4g> = {
  encode(message: Operate4g, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.command !== 0) {
      writer.uint32(8).int32(message.command);
    }
    if (message.body !== 0) {
      writer.uint32(16).int32(message.body);
    }
    if (message.msgId !== 0) {
      writer.uint32(24).int32(message.msgId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Operate4g {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOperate4g();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.command = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.body = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.msgId = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<Operate4g>, I>>(base?: I): Operate4g {
    return Operate4g.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Operate4g>, I>>(object: I): Operate4g {
    const message = createBaseOperate4g();
    message.command = object.command ?? 0;
    message.body = object.body ?? 0;
    message.msgId = object.msgId ?? 0;
    return message;
  },
};

function createBaseOperate4gComplex(): Operate4gComplex {
  return { command: 0, body: "", msgId: 0 };
}

export const Operate4gComplex: MessageFns<Operate4gComplex> = {
  encode(message: Operate4gComplex, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.command !== 0) {
      writer.uint32(8).int32(message.command);
    }
    if (message.body !== "") {
      writer.uint32(18).string(message.body);
    }
    if (message.msgId !== 0) {
      writer.uint32(24).int32(message.msgId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Operate4gComplex {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOperate4gComplex();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.command = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.body = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.msgId = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<Operate4gComplex>, I>>(base?: I): Operate4gComplex {
    return Operate4gComplex.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Operate4gComplex>, I>>(object: I): Operate4gComplex {
    const message = createBaseOperate4gComplex();
    message.command = object.command ?? 0;
    message.body = object.body ?? "";
    message.msgId = object.msgId ?? 0;
    return message;
  },
};

function createBaseChargeStatus(): ChargeStatus {
  return { enableCharge: false };
}

export const ChargeStatus: MessageFns<ChargeStatus> = {
  encode(message: ChargeStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.enableCharge !== false) {
      writer.uint32(8).bool(message.enableCharge);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ChargeStatus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChargeStatus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.enableCharge = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ChargeStatus>, I>>(base?: I): ChargeStatus {
    return ChargeStatus.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ChargeStatus>, I>>(object: I): ChargeStatus {
    const message = createBaseChargeStatus();
    message.enableCharge = object.enableCharge ?? false;
    return message;
  },
};

function createBaseChargeSetting(): ChargeSetting {
  return { chargePower: 0 };
}

export const ChargeSetting: MessageFns<ChargeSetting> = {
  encode(message: ChargeSetting, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.chargePower !== 0) {
      writer.uint32(8).int32(message.chargePower);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ChargeSetting {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChargeSetting();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.chargePower = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ChargeSetting>, I>>(base?: I): ChargeSetting {
    return ChargeSetting.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ChargeSetting>, I>>(object: I): ChargeSetting {
    const message = createBaseChargeSetting();
    message.chargePower = object.chargePower ?? 0;
    return message;
  },
};

function createBasePreference(): Preference {
  return { maximumSpeedLimit: 0 };
}

export const Preference: MessageFns<Preference> = {
  encode(message: Preference, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.maximumSpeedLimit !== 0) {
      writer.uint32(8).int32(message.maximumSpeedLimit);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Preference {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePreference();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.maximumSpeedLimit = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<Preference>, I>>(base?: I): Preference {
    return Preference.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Preference>, I>>(object: I): Preference {
    const message = createBasePreference();
    message.maximumSpeedLimit = object.maximumSpeedLimit ?? 0;
    return message;
  },
};

function createBaseDisplay(): Display {
  return { distance: 0, temperature: 0, time: 0, languageType: 0 };
}

export const Display: MessageFns<Display> = {
  encode(message: Display, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.distance !== 0) {
      writer.uint32(8).int32(message.distance);
    }
    if (message.temperature !== 0) {
      writer.uint32(16).int32(message.temperature);
    }
    if (message.time !== 0) {
      writer.uint32(24).int32(message.time);
    }
    if (message.languageType !== 0) {
      writer.uint32(32).int32(message.languageType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Display {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDisplay();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.distance = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.temperature = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.time = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.languageType = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<Display>, I>>(base?: I): Display {
    return Display.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Display>, I>>(object: I): Display {
    const message = createBaseDisplay();
    message.distance = object.distance ?? 0;
    message.temperature = object.temperature ?? 0;
    message.time = object.time ?? 0;
    message.languageType = object.languageType ?? 0;
    return message;
  },
};

function createBasePatchObtainInfoControl(): PatchObtainInfoControl {
  return { groupId: 0 };
}

export const PatchObtainInfoControl: MessageFns<PatchObtainInfoControl> = {
  encode(message: PatchObtainInfoControl, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.groupId !== 0) {
      writer.uint32(8).int32(message.groupId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PatchObtainInfoControl {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePatchObtainInfoControl();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.groupId = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<PatchObtainInfoControl>, I>>(base?: I): PatchObtainInfoControl {
    return PatchObtainInfoControl.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PatchObtainInfoControl>, I>>(object: I): PatchObtainInfoControl {
    const message = createBasePatchObtainInfoControl();
    message.groupId = object.groupId ?? 0;
    return message;
  },
};

function createBasePatchObtainInfoResult(): PatchObtainInfoResult {
  return { chargerConnState: 0, chargState: 0 };
}

export const PatchObtainInfoResult: MessageFns<PatchObtainInfoResult> = {
  encode(message: PatchObtainInfoResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.chargerConnState !== 0) {
      writer.uint32(8).int32(message.chargerConnState);
    }
    if (message.chargState !== 0) {
      writer.uint32(16).int32(message.chargState);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PatchObtainInfoResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePatchObtainInfoResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.chargerConnState = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.chargState = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<PatchObtainInfoResult>, I>>(base?: I): PatchObtainInfoResult {
    return PatchObtainInfoResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PatchObtainInfoResult>, I>>(object: I): PatchObtainInfoResult {
    const message = createBasePatchObtainInfoResult();
    message.chargerConnState = object.chargerConnState ?? 0;
    message.chargState = object.chargState ?? 0;
    return message;
  },
};

function createBaseCommandResult(): CommandResult {
  return { result: 0, errRes: 0 };
}

export const CommandResult: MessageFns<CommandResult> = {
  encode(message: CommandResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.result !== 0) {
      writer.uint32(8).int32(message.result);
    }
    if (message.errRes !== 0) {
      writer.uint32(16).int32(message.errRes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CommandResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCommandResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.result = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.errRes = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<CommandResult>, I>>(base?: I): CommandResult {
    return CommandResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CommandResult>, I>>(object: I): CommandResult {
    const message = createBaseCommandResult();
    message.result = object.result ?? 0;
    message.errRes = object.errRes ?? 0;
    return message;
  },
};

function createBaseCommandResult2(): CommandResult2 {
  return { result: 0, errorCode: 0 };
}

export const CommandResult2: MessageFns<CommandResult2> = {
  encode(message: CommandResult2, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.result !== 0) {
      writer.uint32(8).int32(message.result);
    }
    if (message.errorCode !== 0) {
      writer.uint32(16).int32(message.errorCode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CommandResult2 {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCommandResult2();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.result = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.errorCode = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<CommandResult2>, I>>(base?: I): CommandResult2 {
    return CommandResult2.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CommandResult2>, I>>(object: I): CommandResult2 {
    const message = createBaseCommandResult2();
    message.result = object.result ?? 0;
    message.errorCode = object.errorCode ?? 0;
    return message;
  },
};

function createBaseTheme(): Theme {
  return { themes: 0 };
}

export const Theme: MessageFns<Theme> = {
  encode(message: Theme, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.themes !== 0) {
      writer.uint32(8).int32(message.themes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Theme {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTheme();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.themes = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<Theme>, I>>(base?: I): Theme {
    return Theme.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Theme>, I>>(object: I): Theme {
    const message = createBaseTheme();
    message.themes = object.themes ?? 0;
    return message;
  },
};

function createBaseNavi(): Navi {
  return { direction: 0, distance: 0 };
}

export const Navi: MessageFns<Navi> = {
  encode(message: Navi, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.direction !== 0) {
      writer.uint32(8).int32(message.direction);
    }
    if (message.distance !== 0) {
      writer.uint32(16).int32(message.distance);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Navi {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNavi();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.direction = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.distance = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<Navi>, I>>(base?: I): Navi {
    return Navi.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Navi>, I>>(object: I): Navi {
    const message = createBaseNavi();
    message.direction = object.direction ?? 0;
    message.distance = object.distance ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
